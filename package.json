{
  "name": "lidertec-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site LíderTec – Cursos Técnicos e Educação Superior",
  "scripts": {
    "dev": "live-server --port=8000 --open=./index.html",
    "start": "live-server --port=8000 --open=./index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
